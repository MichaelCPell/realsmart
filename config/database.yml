# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgis
  encoding: unicode
  pool: 5
  timeout: 5000
  database: realsmart
  schema_search_path: "geom,demog,public"

development:
  <<: *default